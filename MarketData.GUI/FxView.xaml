<UserControl x:Class="MarketData.GUI.FxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" x:Name="FxControl">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <DataGrid Name="FxDataGrid" DockPanel.Dock="Left" Grid.Column="0" AlternationCount="2" ItemsSource="{Binding Path=FxItems}" AutoGenerateColumns="False" >
            <i:Interaction.Behaviors>
            </i:Interaction.Behaviors>
            <DataGrid.ContextMenu>
                <ContextMenu >
                    <MenuItem Header="Menu1"/>
                    <MenuItem Header="Menu2"/>
                    <MenuItem Header="Menu3"/>
                    <MenuItem Header="Menu4"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Currency 1" Binding="{Binding Currency1}"/>
                <DataGridTextColumn Header="Currency 2" Binding="{Binding Currency2}"/>
                <DataGridTextColumn Header="Rate" Binding="{Binding Rate}"/>
                <DataGridTextColumn Header="Update Time" Binding="{Binding UpdateTime}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Expander BorderThickness="1" BorderBrush="SlateGray" IsExpanded="false" DockPanel.Dock="Right" ExpandDirection="Left" Grid.Column="1">
            <DataGrid Name="ColumnChooserDataGrid" AutoGenerateColumns="False" ItemsSource="{Binding ElementName=FxDataGrid, Path=Columns}" HorizontalAlignment="Center" VerticalContentAlignment="Center" >
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Select" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <ItemContainerTemplate>
                                <CheckBox x:Name="SomeCheckBox" 
                                            HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            IsChecked="{Binding Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Path=DataContext.Visibility, Converter={StaticResource  BooleanToVisibilityConverter}, FallbackValue=True}"/>
                            </ItemContainerTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Field" Binding="{Binding  Header}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Expander>
    </Grid>
</UserControl>